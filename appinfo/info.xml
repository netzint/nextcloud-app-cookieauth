<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>nextcloud-app-cookieauth</id>
    <name>Cookie Auth</name>
    <summary>Auto-login users via JWT token from cookie</summary>
    <description><![CDATA[
This app allows automatic Single-Sign-On for users when Nextcloud is embedded in an iframe.
It reads a JWT token from a configurable cookie, validates it against a public key,
and automatically logs in the matching Nextcloud user.

Ideal for embedding Nextcloud in portal applications that use Keycloak or similar OIDC providers.

## Configuration

Configure via **Admin Settings > Security > Cookie Auth**, or add to config.php:

```php
// For iframe SSO - REQUIRED
'session_cookie_samesite' => 'None',

'nextcloud-app-cookieauth' => [
    'realm_url' => 'https://your-edulution-domain.com/auth/realms/edulution',
    'cookie_name' => 'authToken',
    'user_claim' => 'preferred_username',
],
```
    ]]></description>
    <version>1.5.0</version>
    <licence>agpl</licence>
    <author mail="dennis.boelling@netzint.de">Netzint GmbH</author>
    <namespace>CookieAuth</namespace>
    <category>security</category>
    <bugs>https://github.com/netzint/nextcloud-app-cookieauth/issues</bugs>
    <website>https://github.com/netzint/nextcloud-app-cookieauth</website>
    <repository type="git">https://github.com/netzint/nextcloud-app-cookieauth.git</repository>
    <dependencies>
        <nextcloud min-version="25" max-version="32"/>
    </dependencies>

    <!-- Admin Settings Registration -->
    <settings>
        <admin>OCA\CookieAuth\Settings\Admin</admin>
    </settings>
</info>
